# Large runtime-cached files — generated on first boot from committed
# `vocabulary.json` via OpenAI embeddings. Regenerated by
# server/utils/semanticGenerate.ts when missing (Nitro warmup plugin).
embeddings.json
embeddings.f32
embeddings.meta.json
percentiles.json

# Backup artifacts left by the Python generator
*.bak
