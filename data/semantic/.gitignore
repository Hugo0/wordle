# All semantic data now lives in Postgres (pgvector).
# These files are kept on disk as seed data source / archive only.
# Only valid_words.json is still loaded at runtime (spellcheck).
embeddings.json
embeddings.f32
embeddings.meta.json
percentiles.json
axes.json
pca2d.json
umap.json
targets.json
vocabulary.json
*.bak
